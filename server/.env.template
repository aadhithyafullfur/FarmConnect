# FarmConnect Server Configuration Template
# Copy this file to .env and update with your actual values

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# Get your MongoDB connection string from MongoDB Atlas
# 1. Go to https://cloud.mongodb.com/
# 2. Create a cluster (free tier available)
# 3. Create a database user
# 4. Get connection string and replace the placeholders below

MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/FARMCONNECT?retryWrites=true&w=majority&ssl=true&authSource=admin

# ==========================================  
# SERVER CONFIGURATION
# ==========================================
# Port for the server to run on (default: 5001)
PORT=5001

# ==========================================
# SECURITY CONFIGURATION  
# ==========================================
# JWT Secret key for token signing
# Generate a secure random string (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-complex

# ==========================================
# DEVELOPMENT SETTINGS
# ==========================================
# Environment mode (development/production)
NODE_ENV=development

# Enable detailed logging (true/false)  
DEBUG=true

# ==========================================
# INSTRUCTIONS
# ==========================================
# 1. Replace 'username:password' with your MongoDB Atlas credentials
# 2. Replace 'cluster' with your actual cluster name
# 3. Generate a secure JWT_SECRET using the command above
# 4. Save this file as '.env' in the server directory
# 5. Never commit .env to version control (already in .gitignore)

# Example of properly configured values:
# MONGODB_URI=mongodb+srv://farmuser:mypassword123@farmcluster.ab1cd.mongodb.net/FARMCONNECT?retryWrites=true&w=majority&ssl=true&authSource=admin
# JWT_SECRET=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456